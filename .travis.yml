################################################################################
### Generated with R package kwb.pkgbuild v0.1.5
### (installed from 'local' source code on 2020-07-03)
### by calling kwb.pkgbuild::use_pkg(author, description, version = "0.0.0.9000", 
###    stage = "experimental")
### (file created on: 2020-11-11 10:33:23)
################################################################################


language: r
pandoc_version: 2.7.3
sudo: required
cache: packages
r_packages:
- remotes
- covr
matrix:
  include:
  - r: devel
  - r: release
    after_success:
    - Rscript -e 'covr::codecov()'
    before_deploy:
    - Rscript -e 'remotes::install_cran("pkgdown")'
    deploy:
      provider: script
      script: Rscript -e 'pkgdown::deploy_site_github(verbose = TRUE)'
      skip_cleanup: 'true'
      on:
        branch:
        - master
        - dev
  - r: oldrel
